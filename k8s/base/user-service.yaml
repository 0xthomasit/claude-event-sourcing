apiVersion: apps/v1
kind: Deployment
metadata:
  name: user-service
  namespace: shopping
spec:
  replicas: 1
  selector:
    matchLabels:
      app: user-service
  template:
    metadata:
      labels:
        app: user-service
    spec:
      containers:
        - name: user-service
          image: shopping/user-service:latest
          ports:
            - containerPort: 8087
          env:
            - name: EUREKA_HOST
              valueFrom:
                configMapKeyRef:
                  name: shopping-config
                  key: EUREKA_HOST
            - name: REDIS_HOST
              valueFrom:
                configMapKeyRef:
                  name: shopping-config
                  key: REDIS_HOST
            - name: POSTGRES_USER_HOST
              valueFrom:
                configMapKeyRef:
                  name: shopping-config
                  key: POSTGRES_USER_HOST
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: shopping-secrets
                  key: POSTGRES_PASSWORD
          resources:
            requests:
              memory: "512Mi"
              cpu: "250m"
            limits:
              memory: "1Gi"
              cpu: "1000m"
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8087
            initialDelaySeconds: 25
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8087
            initialDelaySeconds: 45
            periodSeconds: 15
---
apiVersion: v1
kind: Service
metadata:
  name: user-service
  namespace: shopping
spec:
  selector:
    app: user-service
  ports:
    - port: 8087
      targetPort: 8087