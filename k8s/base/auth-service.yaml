apiVersion: apps/v1
kind: Deployment
metadata:
  name: auth-service
  namespace: shopping
spec:
  replicas: 1
  selector:
    matchLabels:
      app: auth-service
  template:
    metadata:
      labels:
        app: auth-service
    spec:
      containers:
        - name: auth-service
          image: shopping/auth-service:latest
          ports:
            - containerPort: 8089
          env:
            - name: EUREKA_HOST
              valueFrom:
                configMapKeyRef:
                  name: shopping-config
                  key: EUREKA_HOST
            - name: REDIS_HOST
              valueFrom:
                configMapKeyRef:
                  name: shopping-config
                  key: REDIS_HOST
            - name: POSTGRES_AUTH_HOST
              valueFrom:
                configMapKeyRef:
                  name: shopping-config
                  key: POSTGRES_AUTH_HOST
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: shopping-secrets
                  key: POSTGRES_PASSWORD
          resources:
            requests:
              memory: "512Mi"
              cpu: "250m"
            limits:
              memory: "1Gi"
              cpu: "1000m"
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8089
            initialDelaySeconds: 25
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8089
            initialDelaySeconds: 45
            periodSeconds: 15
---
apiVersion: v1
kind: Service
metadata:
  name: auth-service
  namespace: shopping
spec:
  selector:
    app: auth-service
  ports:
    - port: 8089
      targetPort: 8089