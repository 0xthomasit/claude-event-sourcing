apiVersion: apps/v1
kind: Deployment
metadata:
  name: promotion-service
  namespace: shopping
spec:
  replicas: 1
  selector:
    matchLabels:
      app: promotion-service
  template:
    metadata:
      labels:
        app: promotion-service
    spec:
      containers:
        - name: promotion-service
          image: shopping/promotion-service:latest
          ports:
            - containerPort: 8090
          env:
            - name: EUREKA_HOST
              valueFrom:
                configMapKeyRef:
                  name: shopping-config
                  key: EUREKA_HOST
            - name: KAFKA_BOOTSTRAP_SERVERS
              valueFrom:
                configMapKeyRef:
                  name: shopping-config
                  key: KAFKA_BOOTSTRAP_SERVERS
            - name: POSTGRES_PROMOTION_HOST
              valueFrom:
                configMapKeyRef:
                  name: shopping-config
                  key: POSTGRES_PROMOTION_HOST
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: shopping-secrets
                  key: POSTGRES_PASSWORD
          resources:
            requests:
              memory: "512Mi"
              cpu: "250m"
            limits:
              memory: "1Gi"
              cpu: "1000m"
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8090
            initialDelaySeconds: 25
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8090
            initialDelaySeconds: 45
            periodSeconds: 15
---
apiVersion: v1
kind: Service
metadata:
  name: promotion-service
  namespace: shopping
spec:
  selector:
    app: promotion-service
  ports:
    - port: 8090
      targetPort: 8090