apiVersion: apps/v1
kind: Deployment
metadata:
  name: product-service
  namespace: shopping
spec:
  replicas: 1
  selector:
    matchLabels:
      app: product-service
  template:
    metadata:
      labels:
        app: product-service
    spec:
      containers:
        - name: product-service
          image: shopping/product-service:latest
          ports:
            - containerPort: 8086
          env:
            - name: EUREKA_HOST
              valueFrom:
                configMapKeyRef:
                  name: shopping-config
                  key: EUREKA_HOST
            - name: KAFKA_BOOTSTRAP_SERVERS
              valueFrom:
                configMapKeyRef:
                  name: shopping-config
                  key: KAFKA_BOOTSTRAP_SERVERS
            - name: POSTGRES_PRODUCT_HOST
              valueFrom:
                configMapKeyRef:
                  name: shopping-config
                  key: POSTGRES_PRODUCT_HOST
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: shopping-secrets
                  key: POSTGRES_PASSWORD
          resources:
            requests:
              memory: "512Mi"
              cpu: "250m"
            limits:
              memory: "1Gi"
              cpu: "1000m"
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8086
            initialDelaySeconds: 25
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8086
            initialDelaySeconds: 45
            periodSeconds: 15
---
apiVersion: v1
kind: Service
metadata:
  name: product-service
  namespace: shopping
spec:
  selector:
    app: product-service
  ports:
    - port: 8086
      targetPort: 8086