apiVersion: apps/v1
kind: Deployment
metadata:
  name: shipping-service
  namespace: shopping
spec:
  replicas: 1
  selector:
    matchLabels:
      app: shipping-service
  template:
    metadata:
      labels:
        app: shipping-service
    spec:
      containers:
        - name: shipping-service
          image: shopping/shipping-service:latest
          ports:
            - containerPort: 8085
          env:
            - name: EUREKA_HOST
              valueFrom:
                configMapKeyRef:
                  name: shopping-config
                  key: EUREKA_HOST
            - name: KAFKA_BOOTSTRAP_SERVERS
              valueFrom:
                configMapKeyRef:
                  name: shopping-config
                  key: KAFKA_BOOTSTRAP_SERVERS
            - name: MYSQL_SHIPPING_HOST
              valueFrom:
                configMapKeyRef:
                  name: shopping-config
                  key: MYSQL_SHIPPING_HOST
            - name: MYSQL_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: shopping-secrets
                  key: MYSQL_PASSWORD
          resources:
            requests:
              memory: "512Mi"
              cpu: "250m"
            limits:
              memory: "1Gi"
              cpu: "1000m"
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8085
            initialDelaySeconds: 25
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8085
            initialDelaySeconds: 45
            periodSeconds: 15
---
apiVersion: v1
kind: Service
metadata:
  name: shipping-service
  namespace: shopping
spec:
  selector:
    app: shipping-service
  ports:
    - port: 8085
      targetPort: 8085